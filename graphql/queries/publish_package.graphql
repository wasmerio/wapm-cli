mutation PublishPackageMutation($name: String!, $version: String!, $description: String!, $license: String, $readme: String, $fileName:String) {
  publishPackage(input: {
    name: $name,
    version: $version,
    description: $description,
    license: $license,
    readme: $readme,
    file: $fileName,
    clientMutationId: ""
  }) {
    success
    packageVersion {
      version
    }
  }
}
