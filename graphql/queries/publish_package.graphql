mutation PublishPackageMutation($packageName: String!, $fileName:String) {
  publishPackage(input: {
    packageName: $packageName,
    file: $fileName,
    clientMutationId: ""
  }) {
    success
    packageVersion {
      version
    }
  }
}
