mutation PublishPackageMutation($name: String!, $version: String!, $fileName:String) {
  publishPackage(input: {
    name: $name,
    version: $version,
    file: $fileName,
    clientMutationId: ""
  }) {
    success
    packageVersion {
      version
    }
  }
}
